<p id="notice"><%= notice %></p>

<h1>Cargos</h1>

<table>
  <thead>
    <tr>
      <th>Personal</th>
      <th>Cargo</th>
      <th>Desde</th>
      <th>Hasta</th>
      <th>Curso</th>
      <th>Activo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cargos.each do |cargo| %>
      <tr>
        <td><%= cargo.personal_id %></td>
        <td><%= cargo.cargo %></td>
        <td><%= cargo.desde %></td>
        <td><%= cargo.hasta %></td>
        <td><%= cargo.curso %></td>
        <td><%= cargo.activo %></td>
        <td><%= link_to 'Show', cargo %></td>
        <td><%= link_to 'Edit', edit_cargo_path(cargo) %></td>
        <td><%= link_to 'Destroy', cargo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cargo', new_cargo_path %>
